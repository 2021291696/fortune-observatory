# destiny run-all 汇总报告：门0 + 门2（UI 全流程）— 2026-09-23

## 总结论

- **门0（静态审查）**：blocking 2 / warning 7 / suggestion 7 → **2 条 blocking 已修复并部署**，报告见 `20260923-172612-review/review.md`
- **门1（API 层）**：本清单为纯 UI 路径（2026-09-04 拆分约定），门1 N/A；API 层覆盖由 `manifest.yaml` 历史轮次承担
- **门2（UI 全流程，生产真 LLM）**：**5/5 PASS**（fortune_ai 为复跑 2:1 多数，首轮 RED 根因见下）

## 门2 终态

| 路径 | 终态 | 耗时 | 结果目录 |
|------|------|------|---------|
| dream_view | PASS | 183s | 20260923_180041_run |
| tcm_view | PASS | 173s | 20260923_180041_run |
| qimen_view | PASS | 169s | 20260923_181946_run |
| ask_chat | PASS | 157s | 20260923_182934_run |
| fortune_ai | PASS（复跑 2:1） | 183s | 20260923_183743_run |

全部路径断言了：思考折叠条出现（2026-09-23 新口径）→ 收尾落定标记 → 切换视图持久；ask_chat 另含 `{rand4}` 内容级消失探针（回答含随机码→切走→切回→仍在）。

## 本轮发现与修复（已部署生产）

### 修复 1（B1，用户实测"问事内容消失"的根因）
- `DomainAnalysisConsole.tsx`：`fortune-ai-chat-v1` 单键整包 **读 >120KB 直接返回空**（写侧却无字节上限，40 条长回答必破），且超限后新消息 `persistChat` 整槽覆盖 → 历史**永久丢失**
- 修复：写侧 90KB 总预算 + 跨 owner 按最旧消息裁剪（`trimChatStore`），读门槛放宽到 600KB 仅防垃圾数据。commit `d130b10`

### 修复 2（B2）
- `streamReading.ts`：HTTP 非 200 / 重连耗尽两条收尾路径不删 `inflight` 死条目 → 稳定 cacheKey 的面板永久卡错误态、重试失效
- 修复：两处错误终态补 `inflight.delete`。commit `d130b10`

### 门2 过程中暴露的环境/脚本问题（非产品缺陷，已修测试侧）
1. run-all 执行器 goto 20s 硬编码扛不住跨海慢源站 → `timeout_s` 参数化 + networkidle 降为尽力而为（skill 侧）
2. 执行器 `{rand4}` 按出现位置各自随机，同 run 跨变量必失配 → 改为每 run 一值（skill 侧）
3. 首访（无 profile）默认落 chart 门、排盘成功自动跳命盘（App.tsx:531 内置动线）→ 清单补导航与落定同步
4. 场景分发 fork 的读题词未含分支关键词 → 补齐

## 第二轮治理（2026-09-23 晚，梯子恢复后）

已修复并随 `fix(web): 门0 第二轮` 提交部署：
- **W1** AiExplainPanel 错误态展示 snapshot.error 详情（原先只有无字重试按钮）
- **W2** AiChat 按 aiOwner 加 key 重挂，切换用户不再串写消息槽
- **S1** AI 本机缓存写侧 300KB 预算裁剪（与 B1 同病同治）
- **S2** 中医流式正文按 `_ESSAY_CAP−免责框` 预算位裁剪，与非流式口径对齐
- **S3** BirthForm「清空重填」补齐五个受控日期/时间下拉的复位
- **S4** 流式 HTTP 错误 detail 数组（FastAPI 422）转人话，不再渲染 [object Object]
- **W6** 大运起运日恰为 2/29 时 replace(year) 抛 ValueError → 顺延 3/1（`_start_at`），该出生数据不再永久 422
- 验证：`npm run build` 过；`pytest tests/verified tests/differential` 181 passed（bazi 引擎差分锁定不受影响）

仍挂账（需专项/拍板，未擅动）：
- **W5+F1** 确定性断语红线整篇丢弃——安全红线口径不动（AGENTS.md 铁律），改善方向=句级剔除/改写重试+错误文案，待产品拍板
- **W3** 解梦/奇门/中医断线重生成（无会话注册表）——需把 reading 的 StreamSession 契约推广到三端点，专项+测试
- **W4** 限流额度先扣后被流帽拒（RequestGuard 检查顺序）——动安全中间件，需专项
- **W7** 超远期流年童限错宫、**S5** CORS 层序、**S6** 前后端虚岁口径、**S7** 重连后进度条复闪——低频/口径类，下一批
- **F2 已解**：梯子 7890 恢复后跨海吞吐 425KB/s（20 倍），goto 预算 300s 绰绰有余

## 挂账清单（铁律 9：本轮未收口项）

### F1【功能·概率性】运势 AI 撞"确定性断语"红线 → 整篇已计费内容丢弃（W5+W1 组合）
- 证据：20260923_182552_run；服务器日志 `reading safety violation kind=deterministic`（18:31:05，生成 76s 后收尾校验丢弃）；UI 只剩无字"重新生成讲解"按钮
- 复跑 2:1 通过，但同输入可复现（内容依 LLM 输出而定）
- 建议方向（需产品拍板，未擅动安全红线）：确定性断语命中改为句级剔除/重写重试，而非整篇丢弃；错误态至少展示原因文案
- 覆盖缺口：红线命中后 B2 修复保证可重试，但无自动改写重试

### F2【环境】晚高峰跨海直连吞吐 ~20KB/s，goto 需 300s 预算
- 证据：curl 实测 579KB JS 28.5s；服务器本地 175MB/s（纯线路问题）；iKuuuVPN 7890 未开、核心 7892 直连同速
- 建议：跑生产 UI 链避开晚高峰或先开梯子

### 门0 warning 未修项（详见 review.md）
- W1 AI 面板错误不展示详情（F1 依赖此改善）
- W2 切换用户窗口期聊天串写、W3 解梦/奇门/中医无断线会话注册表（断线=重生成双倍计费）、W4 限流额度白烧、W6 2/29 起运日 ValueError、W7 超远期流年错宫
- S1-S7 改进项（含 S1 AI 缓存 400KB 整包拒绝，与 B1 同构，建议下轮同法治理）

## 证据索引
- 门0 报告：`reports/20260923-172612-review/review.md`
- 各轮原始 results.json/artifacts：`reports/2026-09-23_17*` 与 `2026-09-23_18*`
- 修复 commit：`d130b10`（已推 origin、已部署，生产 bundle `index-BJATnzQ1.js` 验证在位）
