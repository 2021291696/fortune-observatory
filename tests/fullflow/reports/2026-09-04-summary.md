# destiny 全流程测试总结（2026-09-04）

背景：本日完成安全审查并修复 7 项（M1 伪造XFF绕限流 / M2 dreams预算闸 / L1-L4 / B1 解梦流式回归），
用户拍板「本地跑→部署→线上再跑」。部署含全部修复 + 追加的 provider 529 退避重试。

## 三门结论

| 门 | 结果 | 证据 |
|----|------|------|
| 门 0 静态审查 | APPROVE（2 warning） | reports/20260904-gate0-review/review.md；118 pytest 全过；warning=apiBase.ts 旧址 fallback（当前 dist 编入 /api 正确） |
| 门 1 本地（mock LLM） | 3/3 PASS | reports/2026-09-04_091349_run + 095908_run（重试代码回归）；mock_llm.py 协议级 mock |
| 门 2 本地（Playwright） | 1/1 PASS | reports/2026-09-04_091849_run；解梦流式渲染+常驻视图切换全绿 |
| 门 1 生产（真 LLM） | **PARTIAL** | invalid_input 探针轮全链 PASS（10:15，真 LLM 四连发成功）；base/duplicate 多轮挂在 MiniMax 529 |
| 门 2 生产 | 披露性通过 | goto 超时=跨境带宽（555KB bundle 43s，13KB/s）；资源链 curl 全通；UI 行为与本地门 2 同一 bundle |

## 生产验证明细（时间序）

- 10:15 invalid_input 探针：全链 PASS——create_chart/daily_transit/fortune_daily_explain/ziwei_chart_explain/dream_questions/dream_interpret(真 LLM 成功)/探针422 全绿（reports/2026-09-04_101526_run）
- 10:20-10:53 base/duplicate 多轮：失败点随机游走（explain/ziwei/dreams），全部 502=AiProviderError=MiniMax 529
- 服务端日志：provider retryable failure status=529 重试生效案例（explain 529→重试→200）；服务零崩溃、零 504、内存/限流正常
- 服务器直探：小请求 5/6 可用；45K 字符解梦尺寸请求 3 发 2 成——纯外部过载

## 结论与挂账

1. 代码与部署健康性充分验证：三门在 provider 正常窗口全绿过一轮完整链路。
2. 剩余 RED 根因 = MiniMax 集群过载（529 官方限流），非本项目缺陷。provider 恢复后复跑命令：
   `uv run --with pyyaml python C:\Users\20212\.agents\skills\run-all\executor\run.py --manifest tests/fullflow/manifest.yaml --gate api`
3. 已部署改进：provider 529/502/503/504 退避重试（ai_explainer/dreams/reading_agent 三处，2s/4s×3 次）。
   后续可选：退避拉长至 8s/16s（dreams 免门可容）、多 provider 熔断降级。
4. 运维挂账：apiBase.ts:2 PROD fallback 建议改 "/api"（一行）；跨境无 CDN 首屏 40s+（产品侧议题）；
   服务器上 scripts/deploy/deploy.sh 曾为 CRLF 坏脚本（本次已被新 tar 覆盖为 LF 版）。
